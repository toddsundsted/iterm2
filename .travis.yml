language: crystal

crystal:
  - latest

script:
  - crystal spec
  - crystal docs

deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  project_name: toddsundsted/iterm2
  on:
    branch: master
  local_dir: docs
